package temp;

import brianRobinson.LogIt;

class myData {
	private String label = "";
	private String data = "";
	
		myData() {}

	public String getLabel() {
		return label;
	}
	
	public String getData() {
		return data;
	}
}


public class myObjects {

	
	
	class myObject {
		myData myData[] = new myObjects.myObject[0][0];

		
		
		// ***********************************************
		public static void init() {
			for (int i = 0; i < data.length && data[i] != null; i++)
				data[i] = "";
			for (int i = 0; i < label.length && label[i] != null; i++)
				label[i] = "";
			for (int i = 0; i < idx.length; i++)
				idx[i] = 0;
			int len = 0;
		}

	}


	// ***********************************************
	void myObjects() {
	}

	// ***********************************************
	void addKeyValuePair(Object... o) {
		System.out.print("load (" + o.length + "):");
		if (o.length / 2 != 0) {
			System.out.print("load ( Failed on Odd Count: " + o.length + "):");
			return;
		}
		int i = 1, row = 0, col = 0;
		for (Object x : o) { // vargs
			if (i != o.length) // 0 based so ++i
			{
				LogIt.logit(x);
				// myData[row][col].label = o[i];
				 myData[row][col] = String.v alueOf(o[i]);
				col++;
				i++;
			}
			System.out.println();
		}
	}

	// ***********************************************
	void print () {
		for (int i = 0; i < data.length && data[i] != null; i++)
			data[i] = "";
		for (int i = 0; i < label.length && label[i] != null; i++)
			label[i] = "";
		for (int i = 0; i < idx.length; i++)
			idx[i] = 0;
		int len = 0;
	}

	
} // END
